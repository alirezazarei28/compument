export const serve = (port: number, filename: string, dir: string) => {
  console.log(
    "serving on port: \n",
    port,
    "serving fetching from \n  ",
    filename,
    "file is in \n",
    dir
  );
};
